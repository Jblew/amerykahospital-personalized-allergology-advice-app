{
    "name": "amerykahospital-personalized-allergology-advice-app",
    "private": true,
    "scripts": {
        "lerna-bootstrap": "lerna bootstrap",
        "build": "npm run lerna-bootstrap",
        "firebase-deploy": "firebase deploy",
        "deploy": "npm run build && npm run firebase-deploy"
    },
    "devDependencies": {
        "firebase": "^6.2.3",
        "firebase-admin": "^8.2.0",
        "lerna": "^3.15.0"
    }
}
